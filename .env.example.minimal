# Memento MCP minimal environment example
# 작성자: 최진호
# 작성일: 2026-03-13

# Server
PORT=57332
SESSION_TTL_MINUTES=60
LOG_DIR=./logs

# Authentication
MEMENTO_ACCESS_KEY=change-me

# PostgreSQL
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=memento
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-me
DATABASE_URL=postgresql://postgres:change-me@localhost:5432/memento

# pgvector가 public이 아닌 별도 스키마에 설치된 경우 지정 (선택).
# 미설정 시 서버 시작 및 마이그레이션에서 pg_extension으로 자동 감지.
# 대부분의 환경에서는 비워두면 된다.
# PGVECTOR_SCHEMA=

# Redis
REDIS_ENABLED=false
# MEMENTO_REDIS_SESSION_FAIL_CLOSED=false
# 기본 true: legacy agentId 사용 경고/계수. 클라이언트 이관 후 false로 strict 모드 적용.
# MEMENTO_ALLOW_LEGACY_UNBOUND_AGENT_SCOPE=true
CACHE_ENABLED=false

# Context anchors (optional; defaults to total=20, reserve=10)
# MEMENTO_CONTEXT_ANCHOR_LIMIT=20
# MEMENTO_CONTEXT_WORKSPACE_ANCHOR_RESERVE=10

# Embedding
# Leave empty for minimal bootstrap. Add later if semantic search is needed.
# EMBEDDING_PROVIDER=openai
# OPENAI_API_KEY=

# NLI
# Leave empty for minimal bootstrap.
# NLI_SERVICE_URL=

# Consolidation (optional; default true preserves existing behavior)
# MEMENTO_AUTO_PROMOTE_ANCHORS=true
